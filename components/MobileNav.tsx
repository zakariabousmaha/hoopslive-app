
import React from 'react';

// We export this as a valid component so that if the build server 
// is using an old version of App.tsx that imports this, it won't crash.
export const MobileNav = () => {
  return null;
};

export default MobileNav;
